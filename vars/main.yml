---

make: |
  make -f {{ makefile | default('Makefile') }}
          {{ targets | default([]) | join(' ') }}
          {% for key, value in params.iteritems() %}
            {{ key ~ '=' ~ value }}
          {% endfor %}
makeq: |
  {{ make + ' -q' }}
